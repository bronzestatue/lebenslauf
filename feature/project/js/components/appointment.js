export function initializeCalendly() {
    const calendlyContainer = document.querySelector('.calendly-container');
    if (!calendlyContainer) return;

    // Calendly widget is loaded via script in index.html
    // Add any additional Calendly specific functionality here
}